# SORA / GameTarget プロジェクトメモ

## 起動画面（2026-09-17）

- ルートの `index.html` はGameTargetの気球教室をもとにしたSORAのホーム。`guide.css` と `png/` の参照画像4点を同梱する。
- SORA内のゲーム・本格モード・IGC・チュートリアルへは相対リンクを使い、ローカル8002番とGitHub Pagesの両方で動かす。
- GameTarget側の原本は変更しない。公開準備と実際のcommit・pushは分けて扱う。

## 現行の構成（2026-09-15）

- 気球ゲームの実装・修正先は、このリポジトリの `prototype/` に一本化する。
- `../Earth/EarthHealth` は観測・分析・風モデルの検証を担当し、ホームページからSORAへ直接リンクする。EarthHealthには `prototype/` を置かない。
- 採用が決まった風モデルは、このリポジトリの `prototype/` へ反映する。
- `../gametarget` はチェイスカーによるマーカー回収ゲームを担当する。全体を同期しない。
- 下記の旧パス・凍結・両リポジトリへの反映方針より、この構成を優先する。
- 変更後は既定 / `?setup=1` / `?a=` / `?dev=1` / `?igc=1` の5入口を確認する。
  `?dev=1` はPDG有無、`?igc=1` は見る・飛ぶ・なぞるを含める。
- 風モデルの正式化は別工程。EarthHealthの `仕様検討メモ.md` の2026-08-25見直しを参照する。
- ローカル確認はSORAを8002番で配信する。`tutorial.html` は別ページとして保持する。

## 地上クルーの統合（2026-09-16）

- `?dev=1` の離陸前に「地上クルーあり」を選択する。既定はオフ。`road=1` は選択状態の共有用で、devなし・IGCでは有効にしない。
- GameTarget由来の `road.js` / `chasecar.js` と、SORA用の `groundCrew.js` を使用。白は気球追尾、青はJDGのターゲットまたはPDGの宣言目標付近へ先行する。
- 車は保存した離陸地点から配置し、青の100m進入制限と両車のDEM境界制限を守る。道路取得は飛行を待たせない。
- 無線はCキーまたはパネルで問い合わせる。両車の現在地のゲーム内地上風を報告する。道路の取得失敗・未取得・上限到達を「道がない」と断定しない。
- 街並み・車内視点・黄色い操作車・回収採点は今回の統合対象外。GameTargetとEarthHealthは変更していない。
- 検証手順は `experiments/ground-crew/README.md`。既存5入口に加えて、クルーありのJDG/PDG・近距離開始・道路取得失敗も確認する。

作業を始める前に、以下を必ず読んでください:

- 理念.md (公表用: プロジェクトの理念。何のために作っているか)
- 仕様検討メモ.md (SORA本体の仕様。分量が多いので目次的に拾う)
- GameTarget風モデル検討.md (開発途中版の風モデル)
- tmp/検討履歴.md (非公開: これまでの検討・決定の時系列ログ。**全体像を掴むならまずこれ**)
- tmp/理念-内省メモ.md (非公開: 理念との向き合い方についての内省)
- tmp/次回検討項目.md (非公開: 全体見取り図)
- tmp/地球の健康診断構想.md (非公開: 地震モニターとの連携構想)
- tmp/Git運用メモ.md (非公開: push手順。**現行方針が変わっているので必読**)

## 現在の状況(2026-08-22時点)

- **リポジトリ運用(作業ディレクトリで判断)**:
  - `c:\Codex\game` = GameTarget用 → push先は **gametarget**
  - `c:\Codex\rgame2\SORA` = SORA用 → push先は **origin(SORA)**
- **開発の焦点**: `?dev=1`(devMode)の風モデル。気圧配置(H・L)モデル + 日周期変動 + 実データ連携
- **実データ連携**: Open-Meteo から18項目中11項目を実装済み(第1〜5段階)。
  **既定の飛行挙動は一度も変えていない** — すべて「表示のみ」か「ボタンで明示的に適用したときだけ効く」形
- **次の候補**: 地上クルー機能、H・Lデータの保存方式、追加タスク種別(PDG・FIN等)、実機スマホ確認。
  実データの残り7項目はいずれも補助的で優先度は高くない

## 進め方の原則(この案件で繰り返し効いたこと)

- **実装前に実データで確かめる**。CAPEの日変化、日射量の予測力、ガストの閾値、日周期係数の
  ばらつきなど、思い込みが実際に何度も外れている
- **根拠のない数値を基準のように見せない**。明確な基準がない項目は合否を判定せず注意喚起にとどめる
  (理念「誤情報で混乱を生まない」)
- **既定の挙動を壊さない**。物理計算に関わる変更は、既定値を変えずボタンで任意適用にする
- 変更後は4つの入口(既定 / `?setup=1` / `?a=` / `?dev=1`)で回帰確認する

## ローカル動作確認

```
python -m http.server 8000 --directory c:\Codex\rgame2\SORA
```
→ http://localhost:8000/prototype/?dev=1
